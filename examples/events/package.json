{
    "name": "@meshobj/events-example",
    "version": "0.1.0",
    "private": true,
    "main": "dist/index.js",
    "scripts": {
        "start": "node dist/src/index.js",
        "build": "tsc",
        "test": "echo 'Tests moved to slow-test to avoid running in CI' && exit 0",
        "slow-test": "vitest --passWithNoTests --run --hookTimeout=120000"
    },
    "dependencies": {
        "@meshobj/cli": "workspace:*",
        "@meshobj/common": "workspace:*",
        "@meshobj/mongo_repo": "workspace:*",
        "@meshobj/server": "workspace:*",
        "axios": "^1.6.7",
        "js-yaml": "^4.1.0",
        "kafkajs": "^2.2.4"
    },
    "devDependencies": {
        "@meshobj/graphlette": "workspace:^",
        "@testcontainers/kafka": "^11.7.2",
        "@testcontainers/mongodb": "^11.7.2",
        "@types/node": "^20.0.0",
        "mongodb": "^6.20.0",
        "openapi-client-axios": "^7.5.5",
        "testcontainers": "^10.18.0",
        "typescript": "^5.0.0",
        "vitest": "^3.0.5"
    }
}
