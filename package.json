{
  "name": "meshql",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "dependencies": {
    "pnpm": "^9.15.3"
  },
  "devDependencies": {
    "lcov-result-merger": "^5.0.1"
  }
}
