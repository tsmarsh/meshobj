{
  "name": "meshql",
  "private": true,
  "workspaces": [
    "core/*",
    "storage/*",
    "authproviders/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "dependencies": {
    "pnpm": "^9.15.3"
  }
}
