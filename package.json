{
  "name": "meshql",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "0.1.0",
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "dependencies": {
    "@meshql/auth": "workspace:^",
    "pnpm": "^9.15.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "lcov-result-merger": "^5.0.1",
    "vitest": "^2.1.8"
  },
  "packageManager": "yarn@4.6.0"
}
